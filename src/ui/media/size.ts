const baseSize = 4;

export enum Size {
    '1x' = baseSize,
    '1.5x' = baseSize * 1.5,
    '2x' = baseSize * 2,
    '2.5x' = baseSize * 2.5,
    '3x' = baseSize * 3,
    '3.5x' = baseSize * 3.5,
    '4x' = baseSize * 4,
    '5x' = baseSize * 5,
    '6x' = baseSize * 6,
    '8x' = baseSize * 8,
    '9x' = baseSize * 9,
    '10x' = baseSize * 10,
    '11x' = baseSize * 11,
    '12x' = baseSize * 12,
    '13x' = baseSize * 13,
    '16x' = baseSize * 16,
}
